# Gemini API Key for clinical data extraction
# Required for live AI extraction. If absent, the app falls back to deterministic extraction.
GEMINI_API_KEY=

# Optional: Override the Gemini model (defaults to gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
